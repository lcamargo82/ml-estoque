# Copy this file to keystore.properties and fill values locally.
# Never commit the populated keystore.properties file.
storeFile=/absolute/path/to/upload-key.keystore
storePassword=replace-locally
keyAlias=replace-locally
keyPassword=replace-locally
